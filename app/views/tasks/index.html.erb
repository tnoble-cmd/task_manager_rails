

<h1>All Tasks</h1>
<%#erb tags are used to embed Ruby code in HTML files%>
<% @tasks.each do |task| %>
    <h3><a href ="/tasks/<%= task.id %>"> <%= task.title %></a></h3>
    <p><%= task.description %></p>
<% end %>